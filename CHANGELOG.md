# 更新记录 (CHANGELOG)

本文件记录 PhoBamboo v2 重构以来的主要更新。日期格式 YYYY-MM-DD。

---

## 2026-06-06

### 📊 独立销售/客户大屏（/insights）
- 新增**独立大屏页面** `https://phobamboo.fr/insights`，**不在后台 /admin 下**，
  有**单独的用户名/密码**（默认 `patron/patron`，可改 SEED_VIEWER_*）。
- 只读、与后台权限隔离：viewer 账号**只能看大屏**，碰不到后台任何操作；
  后台账号也无法用这套接口（独立 JWT scope）。
- 内容：今日营业额/订单、总营业额、会员数/积分、现金/刷卡拆分、近 14 天营收
  柱状图、畅销菜、**最佳客户**（消费额/到店次数/积分）、最近成交，**每分钟自动刷新** + 实时时钟。
- **经营 KPI（最佳实践）**：客单价、平均每单件数、本周/本月营收、**高峰时段图**、
  堂食vs外带占比、会员vs游客占比、**回头客率**、7天新会员、取消率。
- 适合放收银后方的电视/平板当"经营大屏"，或给老板/投资人远程看。

---

## 2026-06-05

### 🌐 上线 phobamboo.fr（域名 + HTTPS）
- 后端支持 `HOST` 绑定（上线后只监听 127.0.0.1，4000 不再对公网暴露）。
- DEPLOY.md 增加完整域名流程：Cloudflare 灰云 A 记录 → CloudPanel 反代
  （含 WebSocket 段）→ Let's Encrypt SSL → 安全收紧。

### 🧰 桌号二维码生成器（后台 QR 页）
- 批量生成各桌二维码（指向 `/order?table=N`），可选「外带」无桌号码。
- **中心嵌入餐厅 logo**（H 容错，扫码不受影响），下方显示网址。
- 预览 + 单张 PNG 下载 + **打印全部**（美化卡片：绿框圆角、桌号徽章、
  中法双语「扫码点餐 / Scannez pour commander」、网址）。

### 🖨️ 打印优化
- 主页「Menu PDF」重做：logo 抬头、绿色分类栏、**每道菜带缩略图**、
  价格、子分类，打印前等图片加载完成。
- 小票打印对顾客备注做了 HTML 转义（见安全）。

### 📲 PWA「添加到主屏」分区图标
- 点餐页 → 图标直达 `/order`；后台页 → 图标直达 Comptoir；首页 → 首页。
  各用独立 manifest（start_url 不同），互不干扰。

### 📣 短信营销改为「手动发」
- 去掉短信网关（Twilio/HTTP）。统计页改为列出**同意接收短信的会员**，
  老板在手机上点「SMS」用自带短信 App 发（号码+文案预填），或一键复制号码。

### 🔄 其它体验
- 柜台、统计页加**刷新按钮**。
- 柜台订单卡**桌号放大**，无桌号显示「À emporter 外带」。
- 修复 iPhone 上点餐页标题与桌号重叠。
- 修复套餐「前菜选项」在点餐页不显示（后端解析返回前菜详情）。
- multer 升级 1.x→2.x（修安全弃用告警）；补 `mobile-web-app-capable` meta。

### 🔒 安全审查修复
- **会员信息泄露**：公开的 `/api/members` 接口只返回 `{phone, points}`，
  不再暴露姓名/短信同意（姓名改由管理员鉴权接口提供）。
- **打印 XSS**：小票/菜单打印对顾客备注及字段做 HTML 转义。
- `JWT_SECRET` 未设置时启动告警。

---

## 2026-06-04

### 🎉 v2.0 完整重构（从 Firebase 静态网页 → Vue3 全栈）
- **架构**：前端 Vue3 + Vite + Pinia + vue-i18n + Tailwind；后端 Node + Express + Prisma + Socket.IO；数据库 PostgreSQL（VPS 自托管）。
- **数据迁移**：Firebase Firestore → PostgreSQL（菜品 132 / 奖励 3 / 会员 5 / 订单 61）。图片改存 VPS 本地磁盘。
- **多语言**：中 / 英 / 法三语，右上角国旗下拉切换（vue-i18n）。
- **顾客端**：首页菜单浏览 + 扫码点餐（`?table=`）+ 电话会员登录/游客。
- **柜台（平板）**：Socket.IO 实时订单流、结账自动计积分、按电话查/改积分。
- **统计台**：营业额/订单/会员/积分汇总、每日营收图、老板设置（KDS 开关、积分规则）。
- **菜单后台**：菜品/奖励 CRUD、三语名称、套餐配菜、图片压缩上传。
- **KDS 厨房屏**：实时接单 + 声音提醒，可选模块默认关闭。
- **会员积分规则**：满 20€/天累计才按 1:1 计分（配置驱动，可调阈值/倍率）。
- **部署**：单端口架构（后端同时托管前端/API/WebSocket/图片），适配 CloudPanel；含一键脚本 `setup-postgres.sh` / `deploy.sh`、PM2 守护、`DEPLOY.md` 指南。

### 🐛 修复
- **部署建表**：改用 `prisma db push`（仓库无迁移文件，`migrate deploy` 不建表）。
- **后端健壮性**：路由 async 错误统一捕获 + 全局错误中间件 + 进程级兜底，数据库出错不再 crash 整个 Node 进程（此前 Node 15+ 会因未处理 rejection 退出）。

### 💄 顾客端 UI 按原版精确还原（对照 legacy/index.html、order1.html）
- **菜单渲染**：按 `sub_category` 分组带小标题；饮料/酒水改为列表行（非卡片）；菜品卡大图 + 价格浮层 + POP/HOT/VÉGÉ 标签 + NA.png 占位。
- **点单页**：欢迎卡 + 积分组件、底部 Panier/Fidélité 栏、菜品卡 Ajouter 按钮（带数量）、右侧购物车抽屉、套餐弹窗（前菜 +3.80€）、会员弹窗（进度条 + 规则 + 兑换）、GDPR 登录、infos 弹窗、购物车 localStorage 持久化。
- **首页**：Menu PDF 打印、回到顶部按钮。

### 💄 UI 细节修正
- 修复「两个目录栏」+「目录栏贴底线」（CategoryTabs 在移动模式下误渲染桌面胶囊）。
- 饮料/酒水桌面端改两列排列，减少左侧空白。
- 修改桌号改用竹绿风格弹窗（替代浏览器原生 prompt）。
- **Footer 三语化**（地址保留原文）；营业时间下新增「Service sur place à partir de 15€/pers.」。
- 后台 Menu 每个菜品加删除按钮（删除前二次确认）。
- 首页/点单页左上角 SVG 图标替换为 logo.png（取自 `config.logo`）。

### 💄 体验打磨（第二轮）
- Logo 显示优化：先做成白色圆形徽章，后按需求改为透明 + 加大。
- 会员弹窗「Changer」按钮不再被皇冠图标遮挡。
- 新增**全局美化确认弹窗**（ConfirmDialog）和**全局提示**（Toast），
  **替换掉前台 + 后台所有浏览器原生 alert/confirm/prompt**：
  - 后台：柜台结账/取消、菜单删除菜品/奖励
  - 前台：购物车"不登录继续"、下单成功/失败、积分兑换、登录校验

### 📖 文档
- 新增 [使用说明.md](使用说明.md)：给餐馆老板看的通俗中文操作手册。

### 🔒 安全加固 + 运营增强（按最佳实践审查后）
**安全（P0）**
- **订单金额服务端重算**：下单只发商品/奖励 ID + 数量，后端按目录真实价格
  重算总价、套餐补价、积分。杜绝前端篡改金额/刷积分。
- **Socket.IO 鉴权**：连接需带后台 JWT，否则拒绝。订单事件（含会员手机号）
  不再对外公开。
- **登录限流**：`/api/auth/login` 同一 IP 15 分钟最多 10 次，防暴力破解。
- `trust proxy` 设置，配合 CloudPanel 反代取真实 IP。

**运营（P1）**
- **每日订单号**（#1、#2…）：柜台、KDS、小票都显示，方便叫号沟通。
- **柜台新订单声音提醒** + 静音开关。
- **收据/小票打印**：柜台每单可打印（餐厅名/订单号/桌号/明细/总价）。
- **订单计时**：显示已等待分钟数，超过 15 分钟未处理变红高亮。
- **会员资料增强**：可记客户姓名；显示**到店次数、累计消费、最近订单记录**。
- **子分类标题**去掉排序前缀（如「0- Plats」→「Plats」）。
- **套餐补价可配置**（RestaurantConfig.comboPrice，老板设置里可改）。
- **数据库每日自动备份**脚本 `scripts/backup.sh`（保留 14 天）+ cron 说明。

### 📈 统计增强 + PWA
- **畅销菜排行**：统计页新增「最畅销菜品」表（按销量排，含销量/营收），可选时段。
- **CSV 导出**：一键导出已付订单明细（日期/订单号/桌号/电话/金额/积分/菜品），
  带 BOM，Excel 直接打开不乱码。
- **PWA**：可「添加到主屏」当 App 用；菜单/图片离线缓存（vite-plugin-pwa），
  弱网/断网也能看菜单。含 manifest、Service Worker、iOS 图标。

### 💳 支付方式 + 退款 + 短信营销
- **支付方式记录**：柜台结账时选「现金 / 刷卡」，记录到订单；统计页显示
  现金/刷卡营收拆分，CSV 导出含支付方式列。
- **退款流程**：会员消费记录里每单可「退款」→ 订单标记 refunded，
  自动**撤销该单所得积分、退回该单已花积分**。
- **短信营销落地**：老板设置里可向「同意接收短信」的会员**群发营销短信**
  （含测试发送、opt-in 人数）。后端 SMS provider 可插拔（Twilio / 通用 HTTP），
  未配置时为模拟模式（只记日志不真发），配置 env 即真实发送。
