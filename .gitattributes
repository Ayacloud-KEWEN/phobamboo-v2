# Normalize line endings. Shell scripts MUST stay LF so they run on the Linux VPS.
* text=auto
*.sh text eol=lf
*.cjs text eol=lf

# Treat binaries as-is
*.png binary
*.jpg binary
*.jpeg binary
*.webp binary
*.ico binary
*.JPG binary
